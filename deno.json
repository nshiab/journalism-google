{
  "name": "@nshiab/journalism-google",
  "version": "1.1.2",
  "description": "Useful TypeScript functions for journalism projects. This library focuses on accessing Google services.",
  "repository": "https://github.com/nshiab/journalism-google",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "tasks": {
    "all-tests": "deno install && deno fmt --check && deno lint && deno check src/index.ts && deno doc --lint src/index.ts && deno publish --allow-dirty --dry-run && TZ=America/Toronto deno test -A --env-file=.env --fail-fast && deno task llm",
    "llm": "deno run -A jsr:@nshiab/deno-docs-to-md && deno fmt llm.md",
    "test-coverage": "deno test -A --env-file=.env --fail-fast --coverage=cov_profile && deno coverage cov_profile",
    "patch": "deno task all-tests && deno run -A jsr:@nshiab/bump-version patch",
    "minor": "deno task all-tests && deno run -A jsr:@nshiab/bump-version minor",
    "major": "deno task all-tests && deno run -A jsr:@nshiab/bump-version major"
  },
  "exclude": [
    "npm/"
  ],
  "publish": {
    "exclude": [
      "test",
      ".github",
      "npm"
    ]
  },
  "nodeModulesDir": "auto",
  "imports": {
    "@google-cloud/storage": "npm:@google-cloud/storage@^8.0.1",
    "google-auth-library": "npm:google-auth-library@^11.0.2",
    "google-spreadsheet": "npm:google-spreadsheet@^5.3.0"
  },
  "compilerOptions": {
    "lib": [
      "dom",
      "deno.ns"
    ]
  },
  "lint": {
    "include": [
      "src/**/*.ts"
    ]
  }
}
