{
  "name": "@nshiab/journalism-google",
  "version": "0.0.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "tasks": {
    "all-tests": "deno install && deno fmt --check && deno lint && deno check src/index.ts && deno doc --lint src/index.ts && deno publish --allow-dirty --dry-run && TZ=America/Toronto deno test -A --fail-fast && deno task llm",
    "llm": "deno doc --json src/index.ts > docs.json && deno run -A src/generateLlmMd.ts && deno fmt llm.md",
    "test-coverage": "deno test -A --fail-fast --coverage=cov_profile && deno coverage cov_profile",
    "patch-no-tests": "deno run -A src/incrementVersion.ts patch",
    "minor-no-tests": "deno run -A src/incrementVersion.ts minor",
    "patch": "deno task all-tests && deno run -A src/incrementVersion.ts patch",
    "minor": "deno task all-tests && deno run -A src/incrementVersion.ts minor",
    "major": "deno task all-tests && deno run -A src/incrementVersion.ts major"
  },
  "publish": {
    "exclude": [
      "test",
      ".github"
    ]
  },
  "nodeModulesDir": "auto",
  "imports": {
    "@google-cloud/storage": "npm:@google-cloud/storage@^7.18.0",
    "@nshiab/journalism-format": "jsr:@nshiab/journalism-format@^1.0.1",
    "@std/dotenv": "jsr:@std/dotenv@^0.225.5",
    "d3-dsv": "npm:d3-dsv@^3.0.1",
    "google-auth-library": "npm:google-auth-library@^10.5.0",
    "google-spreadsheet": "npm:google-spreadsheet@^5.0.2"
  },
  "compilerOptions": {
    "lib": [
      "dom",
      "deno.ns"
    ]
  },
  "lint": {
    "include": [
      "src/**/*.ts"
    ]
  }
}
